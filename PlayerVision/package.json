{
  "name": "playervision",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp staticwebapp.config.json dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@owlbear-rodeo/sdk": "^3.1.0"
  }
}
